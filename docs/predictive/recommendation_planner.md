# Declutr Recommendation Planner Guide

The `RecommendationPlanner` receives raw pattern predictions, evaluates user confidence thresholds (e.g. min 0.80), filters disabled categories, and formats explainable recommendations with evidence rationales and suggested actions.
